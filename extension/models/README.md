# Local model artifacts

Downloaded ONNX model files are placed here by `scripts/download-models.sh` and
are intentionally ignored by Git. The extension must treat an absent or failed
privacy model as a blocking condition; it must never fall back to sending raw
page or screenshot data.

